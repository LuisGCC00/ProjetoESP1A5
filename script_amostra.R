#Script utilizado para gerar amostra do conjunto de dados.

#source("install_load_packages.R")

#file <- "amostra.csv"
#data <- read.csv(file, stringsAsFactors = FALSE)
#main_df <- data.frame(data)

# amostra <- dados %>%
#   sample_frac(0.1, replace = FALSE, seed = 42)

#write.csv(amostra, file = "amostra_van5_2022.csv", row.names = FALSE)